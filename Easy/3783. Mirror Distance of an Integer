class Solution {
    public int mirrorDistance(int n) {
        int reversion = 0;
        int x = n;

        while (x > 0) {
            int r = x % 10;
            x /= 10;
            reversion = reversion * 10 + r;
        }

        return Math.abs(reversion - n);
    }
}
