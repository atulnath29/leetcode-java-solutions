class Solution 
{
    public double angleClock(int h, int m) 
    {
        return Math.min(Math.abs(30 * h - 5.5 * m), 360 - Math.abs(30 * h - 5.5 * m));
    }
}

/*
    double angleH = 30F *(h + m / 60F);        
    double angleM = 360 * m / 60F;
    double diff = Math.abs(angleH-angleM);
    return (diff > 180) ? 360 - diff : diff;
    */
